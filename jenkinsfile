pipeline{

    agent any

    tools{
        maven 'maven'
    }

    stages{

        

        stage('Build'){
            steps{
                sh 'npm run build'
            }
        }

        stage('test'){
            steps{
                sh 'npm test || echo "No tests defined"'
            }
        }

        stage('dependencies'){
            steps{
                sh 'npm install'
            }
        }

        

        
    }
}